# src/projects/backend/README.md

Instalar as dependências:

```bash
npm install
```

Criar um arquivo .env com a URI de conexão do MongoDB:

```env
MONGO_URI=your_mongo_connection_string
```

Iniciar o servidor:

```bash
npm run dev
```
